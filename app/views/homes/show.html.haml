/ -- BANNER --
.row
  .col-lg-12.col-md-12.col-sm-12.col-xs-12
    .well.well-lg.text-center
      %h1=@home.title
      %p.lead=@home.content
      = link_to 'Edit', edit_home_path(@home), :class => "btn btn-primary"
.row
  - @paintings_group.each do |cat, paintings|
    .col-lg-6.col-md-6.col-sm-6
      %h3.text-muted=cat
      - for painting in paintings
        %p
          =link_to (image_tag painting.image_url(:thumb).to_s, :class => "img-responsive"), paintings_path(:type => cat.downcase)
        %p
          =painting.description




/\|
/= link_to 'Back', homes_path
